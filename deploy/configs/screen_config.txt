# turn off screen blanking
@xset s noblank

# turn off screen saver
@xset s off

# turn off display power management system
@xset -dpms

# hide mouse-cursor when idle
@unclutter -idle 0.5

# run chromium in kiosk mode
@chromium-browser --check-for-update-interval=31536000 --noerrdialogs -disable-pinch --no-context-menu --kiosk http://localhost